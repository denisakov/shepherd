$(document).ready(function () {
  $("#messageModal").on('shown.bs.modal', function(e) {
  var button = $(e.relatedTarget) // Button that triggered the modal
  var recipient = button.data('recipient') // Extract info from data-* attributes
  var subject = button.data('subject')
  // var message = button.data('message')
  // If necessary, you could initiate an AJAX request here (and then do the updating in a callback).
  // Update the modal's content. We'll use jQuery here, but you could use a data binding library or other methods instead.
  var modal = $(this)
  modal.find('.modal-title').text('New message to ' + recipient)
  modal.find('#recipient-name').val(recipient)
  modal.find('#subject-text').val('MV "' + subject +'"')
  // modal.find('#message-text').val(message)
  });
  $("#messageModal").on("click", ".send", function(e) {
      bootbox.alert("Message sent!", function() {
      console.log("Alert Callback");
    });
  });
});
// $(document).ready(function () {
//   $("#messageModal").on("click", ".send", function(e) {
//       bootbox.alert("Message sent!", function() {
//       console.log("Alert Callback");
//     });
//   });
// });